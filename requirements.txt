# core (offline: harness, loop, guide, benchmark, measurement)
pytest>=7.4
pytest-cov>=4.1
# web POC + deploy
fastapi>=0.110
itsdangerous>=2.1          # session cookie signing (fieldcraft_web/auth.py)
uvicorn[standard]>=0.29
httpx>=0.27
