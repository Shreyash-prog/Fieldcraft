# Ticket: truncate_words(text, limit)
Implement `truncate_words` in truncate_words.py so all tests pass. Do not modify the tests.

## Acceptance criteria
- TW1 — Truncates to the word limit and appends an ellipsis.
- TW2 — Never cuts mid-word.
- TW3 — No ellipsis when nothing was removed.
- TW4 — Text exactly at the limit is returned unchanged.
